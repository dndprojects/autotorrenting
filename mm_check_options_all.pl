# options setting for movie master script
$remote_files = "http://";
$movies_dir = "/media/MOVIES";
$tv_series_dir = "/media/TV";
$transmission_dir = "/media/Download";
$send_mail_alias = 'email1 email2';
$transmission_bin = "/cygdrive/d/tools/TransmissionD/bin" ; # for cygwin

# optional setting
# -------------------------------------------------------
# $remote_db = "/tmp/remote_movies.db";
# $local_db = "/media/movies_local.db";
# $remote_files = "http://";
# $check_for_tv = "true";
# $use_iconv = "false"; # recommended for windows users this will change srt Unicode to ANSI
# $use_convert = "false" ; # resize poster to 720x1080 (runtime issue)
# $preferd_poster = "he" ; # options is "he" or "en"
# $http_remote_db  = "${remote_files}/sqlite/movies.db" ;
# $start_transmission_cmd = 'systemctl start transmission';

